#[cfg(test)]
pub mod attackers_tests {
    // use crate::{WasmChess, tsify_structs::square_str::SquareStr};
    // use shakmaty::Square;

    // fn aggregate_attacks(chess: &WasmChess) {
    //     let attackers_vec = [0; 64];

    //     Square::ALL.iter().enumerate().for_each(|(index, sq)| {
    //         let square_str = SquareStr::from_shakmaty_sq(sq);

    //         let attacker_count = chess.attackers(SquareStr::A1, Some(ColorChar::W)).unwrap();
    //     });
    // }
}
